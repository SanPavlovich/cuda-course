/*matrix<float> A(100, 200);
matrix<float> B(200, 300);
auto startTime = chrono::system_clock::now();
matrix<float> C(A * B);
auto endTime = chrono::system_clock::now();
chrono::duration<double> duration = endTime - startTime;
cout << "execution time: " << duration.count() << endl;*/

/*void swapp(size_t& a, size_t& b) {
    cout << "swap" << endl;
    size_t t = a;
    a = b;
    b = t;
    return;
}

size_t a = 0;
size_t b = 1;
cout << "a: " << a << ", " << "b: " << b << endl;
swapp(a, b);
cout << "a: " << a << ", " << "b: " << b << endl;*/

/*matrix<int> G;
cout << G.get_data_pointer() << ", " << (G.get_data_pointer() == nullptr) << endl;
cout << G.get_nrows() << ", " << G.get_ncols() << endl;
matrix<int> A(2, 2);
cout << "A info:" << endl;
cout << A.get_nrows() << ", " << A.get_ncols() << endl;
A.rand_init();
A.print();
G = A;
G.print();
cout << "G info:" << endl;
cout << G.get_data_pointer() << ", " << (G.get_data_pointer() == nullptr) << endl;
cout << G.get_nrows() << ", " << G.get_ncols() << endl;*/

/*class P {
public:
    matrix<int> M;
};
P p;
cout << p.M.get_nrows() << ", " << p.M.get_ncols() << endl;*/

/*matrix<float> A(2, 2);
A.rand_init();
cout << "matrix A:" << endl;
A.print();

matrix<float> B(2, 2);
B.rand_init();
cout << "matrix B:" << endl;
B.print();

A *= B;
cout << "matrix A:" << endl;
A.print();*/

/*matrix<float> A(3, 2);
A.rand_init();
cout << "matrix A:" << endl;
A.print();

cout << "matrix A.T:" << endl;
A.transpose();
A.print();

matrix<float> B(3, 4);
B.rand_init();
cout << "matrix B:" << endl;
B.print();

A *= B;
cout << "matrix A:" << endl;
A.print();

matrix<float> C = A + A;
cout << "matrix C:" << endl;
C.print();*/

/*matrix D = A * B;
cout << "matrix D:" << endl;
D.print();*/